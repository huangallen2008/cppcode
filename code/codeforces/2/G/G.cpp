#include<bits/stdc++.h>
using namespace std;
int T,n,a[100001],k=0;
int d() {
    vector<vector<int>> dp(n,vector<int>(n)) dp;

}
main() {
    cin.tie(0);
    cin.sync_with_stdio(0);
    cin>>T;
    while(T--) {
        cin>>n>>k;
        for(int i=0;i<n;i++) cin>>a[i];

    }
    return 0;
}
